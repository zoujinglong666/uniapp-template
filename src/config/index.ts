// ? 全局默认配置项

// 首页地址（默认）
export const HOME_URL = "/pages/Home/index";

// 登录页地址（默认）
export const LOGIN_URL = "/pages/Login/index";
export const BASE_URL = "http://127.0.0.1:7529/api";

// 默认主题颜色
export const DEFAULT_PRIMARY = "#009688";

// 路由白名单地址（必须是本地存在的路由 staticRouter.ts 中）
export const ROUTER_WHITE_LIST: string[] = ["/500",'/404'];

export const API_URL="http://localhost:8848";

